spring:
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        show_sql: false
    hibernate:
      ddl-auto: none
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:12.4:///animals_test
    username: postgres
    password: minis123
    hikari:
      connection-init-sql: CREATE SCHEMA IF NOT EXISTS "animals";
  liquibase:
    enabled: false